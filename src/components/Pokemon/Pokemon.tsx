import React from 'react';

interface PokemonProps {
  name: string,
}

const Pokemon = (props: PokemonProps) => {
  return <React.Fragment>Hello Poke :D</React.Fragment>;
};

export default Pokemon;
