import React from 'react';
import Button from 'react-bootstrap/Button';

const Header = () => {
  return (
    <React.Fragment>
      <header>
        <nav>
          HEADER
        </nav>
      </header>
    </React.Fragment>
  );
};

export default Header;
