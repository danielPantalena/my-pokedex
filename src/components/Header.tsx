import React from 'react';

const Header = () => {
  return (
    <React.Fragment>
      <header>
        <nav>
          HEADER
        </nav>
      </header>
    </React.Fragment>
  );
};

export default Header;
